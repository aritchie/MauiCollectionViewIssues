<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="CollectionViewIssues.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:CollectionViewIssues"
    Shell.FlyoutBehavior="Disabled"
    Title="CollectionViewIssues">

    <ShellContent
        Title="Dynamic Size Slow"
        ContentTemplate="{DataTemplate local:MainPage}" />

    <ShellContent
        Title="Dynanic Size Fast"
        ContentTemplate="{DataTemplate local:VirtualListViewPage}" />

    <ShellContent
        Title="Grid Columns"
        ContentTemplate="{DataTemplate local:CVGridPage}" />

    <ShellContent
        Title="Sharpnado Columns"
        ContentTemplate="{DataTemplate local:SharpnadoGridPage}" />

</Shell>